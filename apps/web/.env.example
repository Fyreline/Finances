# apps/web/.env.example — copy to apps/web/.env.local for a non-default API base.
# Dev default (auth.ts / api.ts) is http://127.0.0.1:8201 (kakeibo-api dev port).
#VITE_API_BASE=http://127.0.0.1:8201
